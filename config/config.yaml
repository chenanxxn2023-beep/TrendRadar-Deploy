# ═══════════════════════════════════════════════════════════════
#                    TrendRadar 配置文件 (K12教育版)
# ═══════════════════════════════════════════════════════════════

# ===============================================================
# 1. 基础设置
# ===============================================================
app:
  timezone: "Asia/Shanghai"
  show_version_update: true

# ===============================================================
# 2. 数据源 - 热榜平台
# 优化策略：保留大众社会类平台（教育热点多），注释掉纯金融平台
# ===============================================================
platforms:
  - id: "toutiao"
    name: "今日头条"  # 教育政策、社会新闻多
  - id: "baidu"
    name: "百度热搜"  # 家长搜索主力
  # - id: "wallstreetcn-hot"
  #   name: "华尔街见闻" # (已屏蔽：金融属性太强，教育内容少)
  - id: "thepaper"
    name: "澎湃新闻"  # 官方教育通报多
  - id: "bilibili-hot-search"
    name: "bilibili 热搜" # 学生群体聚集
  # - id: "cls-hot"
  #   name: "财联社热门" # (已屏蔽：纯财经)
  - id: "ifeng"
    name: "凤凰网"
  - id: "tieba"
    name: "贴吧"
  - id: "weibo"
    name: "微博"      # 舆情爆发地
  - id: "douyin"
    name: "抖音"      # 教育短视频热点
  - id: "zhihu"
    name: "知乎"      # 教育焦虑、升学讨论聚集地

# ===============================================================
# 3. 数据源 - RSS 订阅
# ===============================================================
rss:
  enabled: true

  # 🔴 核心修改：时效性控制
  freshness_filter:
    enabled: true
    max_age_days: 2   # 【已修改】设置为 2 天，即 48 小时内的热点

  feeds:
    # 建议添加教育相关的 RSS 源（如果没有可暂时保持默认或清空）
    - id: "ruanyifeng"
      name: "阮一峰的网络日志"
      url: "http://www.ruanyifeng.com/blog/atom.xml"
      
    - id: "yahoo-finance"
      name: "雅虎财经"
      url: "https://finance.yahoo.com/news/rssindex"
      enabled: false  # 禁用

# ===============================================================
# 4. 报告模式
# ===============================================================
report:
  mode: "current"                     # 保持 current，适合你的“实时发现灵感”面板
  display_mode: "keyword"             # 🔴 关键：必须是 keyword 模式，才能过滤出教育内容
  rank_threshold: 5
  sort_by_position_first: false
  max_news_per_keyword: 0
  reverse_content_order: false

# ===============================================================
# 5. 推送通知 (根据你的 Cursor 项目需求，这里主要影响后端数据生成)
# ===============================================================
notification:
  enabled: true
  push_window:
    enabled: false
  
  standalone_display:
    enabled: false

  channels:
    # 保持你原有的配置，此处略去敏感信息...
    # 如果你主要通过 API 读取 sqlite/json，这里的推送可以不配
    generic_webhook:
      webhook_url: ""

# ===============================================================
# 6. 存储配置
# ===============================================================
storage:
  backend: "local" # 建议开发阶段强制使用 local
  formats:
    sqlite: true
    txt: false
    html: true
  local:
    data_dir: "output"
    retention_days: 0

# ===============================================================
# 7. AI 分析功能
# ===============================================================
ai_analysis:
  enabled: true
  provider: "deepseek" # 你的截图里是 DeepSeek，保持一致
  api_key: ""          # 请确保填写或使用环境变量
  model: "deepseek-chat"
  timeout: 90
  push_mode: "both"
  max_news_for_analysis: 50
  include_rss: false
  prompt_file: "ai_analysis_prompt.txt"

# ===============================================================
# 8. 高级设置
# ===============================================================
advanced:
  debug: false
  version_check_url: "https://raw.githubusercontent.com/sansan0/TrendRadar/refs/heads/master/version"
  mcp_version_check_url: "https://raw.githubusercontent.com/sansan0/TrendRadar/refs/heads/master/version_mcp"
  
  crawler:
    enabled: true
    request_interval: 2000
    use_proxy: false
    # default_proxy: "http://127.0.0.1:10801" # 如果你在国内抓取困难，可能需要开启代理

  rss:
    request_interval: 1000
    timeout: 15
    notification_enabled: true

  weight:
    rank: 0.6
    frequency: 0.3
    hotness: 0.1

  max_accounts_per_channel: 3
  batch_send_interval: 3
